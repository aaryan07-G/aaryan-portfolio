import type { Testimonial } from "@/types";

/**
 * Intentionally empty until real client/collaborator feedback is available.
 * TestimonialsSection renders a "Coming Soon" state when this array is
 * empty — add real entries here and the section switches over automatically,
 * no component changes needed.
 */
export const testimonials: Testimonial[] = [];
